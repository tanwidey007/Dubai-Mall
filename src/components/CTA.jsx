import SectionWrapper from "./SectionWrapper";

export default function CTA() {
  return (
    <SectionWrapper>
      <section className="cta">
        <h2>Partner With The Future of Retail</h2>
        <p>Join the most iconic destination globally</p>
        <button>Lease a Space</button>
      </section>
    </SectionWrapper>
  );
}